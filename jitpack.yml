# configuration file for building snapshots and releases with jitpack.io
jdk:
  - openjdk11
install:
  - ./gradlew build :android:publishToMavenLocal :androidcamerax:publishToMavenLocal :encryption:publishToMavenLocal :imageprocessor:publishToMavenLocal